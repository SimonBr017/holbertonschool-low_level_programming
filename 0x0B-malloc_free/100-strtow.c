#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * **strtow - split spring into words
 *@str: string
 *Return: null if str = null or str = "", 
 */
char **strtow(char *str)
{
        char **dest;

        dest = 0;
        if (str == '\0' || str == NULL)
        {
                return (NULL);
        }
        return (dest);
}
